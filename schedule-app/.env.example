# フロントエンド（.env）— ブラウザに露出する。anon key と URL のみ。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- 以下はフロントの .env には絶対に置かない ---
# Supabase Edge Function の Secret として設定する（設計原則 2-3）
# supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_API_KEY=
