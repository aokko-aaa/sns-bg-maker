# このリポジトリについて

2つのものが同居している。

- `schedule-app/` — 仕事・家族・個人の予定を横断管理する個人用スケジュールアプリ（Vite + React + Supabase）
- `jibun-no-mise/` — 屋号「じぶんの店」。『Cloudflareの教科書』第1部「道場」で 8 章かけて育てるサイト

以下は主に `jibun-no-mise/` の道場についての取り決め。

## 教材ファイルの置き場

『Cloudflareの教科書』の教材一式（本文全章・`TRAPS.md`・`LIMITS_TABLE.md`）は `kyokasho/` にある。

**このフォルダは `.gitignore` 済み。コミットしないこと。**
購入者特典で再配布不可、かつこのリポジトリは public のため。
コンテナが作り直されると消えるので、無ければ店主に再アップロードを頼む。

## Cloudflare の作業に入る前に

教材 README の掟に従う。

1. **作業を始める前に `kyokasho/TRAPS.md`（罠帖）の該当グループを読んで先回りする。**
   デプロイなら A、D1 なら B、R2 なら C、Turnstile なら D、ドメインなら E、Cron/メールなら F、
   Vercel からの引っ越しなら G、レート制限などの守りなら H、新しく作り始める前は K
2. 症状が出たら、罠帖冒頭の「症状から引く」で当たりを付けてから調べる
3. 料金・無料枠は `kyokasho/LIMITS_TABLE.md` を見てから、公式ドキュメントで最新を確認する（数字は変わる）
4. 手順は各章の「頼み方」原文を型にする。**人間には、名義・お金・秘密の場面だけを頼む**

罠帖は 2026-08-17 版。仕様は変わるので、避け方を適用する前に「いまも同じ挙動か」を確かめる姿勢で。

## 道場の現在地

| 章 | 内容 | 状態 |
|---|---|---|
| 第1章 | 開通。準備中ページを世界に出す | **公開待ち**（下記） |
| 第2章 | 独自ドメインを接続する | 未着手 |
| 第3章 | 作品ギャラリーにする | 未着手 |
| 第4章 | お便り箱（フォーム）を置く | 未着手 |
| 第5章 | お便りを D1 に残す + 店主だけのお便り帳 | 未着手 |
| 第6章 | 作品の絵を R2 に移す | 未着手 |
| 第7章 | Turnstile で関所を置く | 未着手 |
| 第8章 | メール通知 + Cron で日誌 | 未着手 |

### 第1章が「公開待ち」である理由

ページと設定は `jibun-no-mise/` に用意済みで `wrangler deploy --dry-run` まで通っている。
ただし**このリモートセッションのコンテナからは deploy できない**。
`wrangler login` は localhost へのコールバックを待つため、ブラウザの開けない環境では成立しない（罠帖 J-2）。
店主がお手元の PC で `cd jibun-no-mise && ./deploy.sh` を実行すれば完了する。

このコンテナから deploy する必要が出たら、API トークン方式に切り替える
（`CLOUDFLARE_API_TOKEN` を環境変数に設定。手順は `jibun-no-mise/README.md`）。

## デプロイの型

**push しただけでは本番は変わらない**（罠帖 G-5）。Workers は `wrangler deploy` を叩いたときにだけ更新される。
`jibun-no-mise/deploy.sh` が「認証確認 → deploy → 数秒待つ → 実 HTTP で中身を確認」を一続きにしてある。
手で叩くときも、この順番を崩さないこと。

**デプロイ直後の一発目で判定しない**（罠帖 A-1）。配達拠点に行き渡るまで数秒〜十数秒のずれがある。

## 章ごとに先回りすること

- **第2章（ドメイン）**: 独自ドメインを付けると workers.dev の仮 URL は自動で 404 になる（A-2・本文 2-4）。
  故障ではない。ただし仮 URL を配った先があるなら残す設定を明示する。
  外すときは設定ファイルの編集だけでは剥がれず、カスタムドメイン一覧から削除する（A-3）。
  `.jp` は Cloudflare で取得できない。日本の会社で取り DNS だけ預ける（E-1）。
- **第4章（お便り箱）**: 受け口のために `main` を足す。
  `assets.not_found_handling` を触るときは、受け口へのリクエストが Worker に届くかを必ず実測する。
  受け取る窓口は「受け取らないもの」（空・長すぎる）とセットで作る。
- **第5章（D1）**: 鍵はコードに書かず `wrangler secret` へ。送るときは URL ではなくヘッダに入れる。
  まとめ書きのチャンク行数は 1 文の変数上限から計算する。決め打ちの数字を書かない（B-1）。
  コード先行デプロイと migration 未適用の食い違いに注意（B-3）。
  `wrangler d1` の各コマンドは対話確認で止まる（B-4）。
- **第6章（R2）**: 置くときは `--remote` を付ける（C-1）。反映確認に `object get` を使わない（C-2）。
  差し替えは同名上書きせず新しいファイル名で（C-3）。置き場を移したら読む側を全部洗い直す（C-4）。
- **第7章（Turnstile）**: ドメインを足したら許可ホスト名にも追加する（D-2）。
  照合結果はエラー種別ごとログに出す（D-1）。
- **第8章（メール・Cron）**: Cron は UTC。日本時間から 9 時間引く（F-1）。
  検証は一時的に `*/5` にして 1 回見届け、本番の時刻に戻す（F-2）。
  メール送信は Workers 有料プラン（月5ドル）が前提（F-3）。
  ログは `wrangler tail --format json > tail.log`。pretty をパイプに繋がない（A-14）。

## 置き場の使い分け

物差しは1本。**消えたら困るか、数が合わないと困るか。**

- **D1（帳簿）** — 顧客の記録、注文、送信履歴。消えたら商売に響くもの。迷ったらこちらへ
- **R2（倉庫）** — 画像・音声・動画・zip。帳簿に書くには大きすぎるもの。
  配信の転送量は無料なので「配る量が読めないもの」ほど向く。**動画は必ず R2 から配る**
  （Workers の同梱は Range 要求に応えられず、Safari で再生が壊れる／A-5・A-6）。
  倉庫はひとつの巨大な物置にせず、用途ごとに分ける
- **KV（付箋）** — 済/未済のフラグ、一時的なカウンタ、キャッシュ。最悪作り直せるもの。
  読みは太いが書きは細い。毎秒書き込む使い方は、そもそも KV 向きでないサイン

**D1 のまとめ書きは 1 文の bind 変数が上限 100**（公式仕様）。行数×列数がこれを超えると
実行前に即死する。ローカルの模擬環境は上限が緩くテストが素通りするので、
チャンク行数は上限から計算で導き、決め打ちの数字を書かない。

## 自動化するときは

**止まり方まで設計する。** 正常系より異常系に手をかける。

- 遅れたらどうするか（逃げ道を先に決めておく）
- 失敗したとき、誰がどう気づくか。自動化の敵は失敗ではなく「失敗に誰も気づかない」こと
- 直せるものは見張り自身に直させる（機械が1回再試行 → AI → 人間、の三段構え）

## 検収の考え方

**「ページが開く」で終わらせない**（罠帖 G-3）。本物の経路を 1 回通して初めて完了とする。
フォームなら実際に 1 回送る。メールなら実際に 1 通受け取る。

**失敗を catch で握りつぶさない**（A-9）。画面が正常でも裏が全滅していることがある。

**「読める」でなく「書ける」まで確かめる**（B-2）。全ページ 200 でも、書き込み経路だけ
死んでいることがある。読むだけの点検では、書き込みの故障は原理的に見つからない。

引っ越しの検収は、目視の「だいたい同じ」ではなくファイルの指紋（ハッシュ）の全数一致で言い切る。

## git

作業ブランチは `claude/wrangler-cloudflare-setup-s68wii`。
PR は明示的に頼まれたときだけ作る。
